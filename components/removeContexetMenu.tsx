"use client";

import { useEffect } from "react";

const RemoveContexetMenu = () => {
    // useEffect(() => {
    //   document.addEventListener("contextmenu", (e) => e.preventDefault());

    //   return () => {
    //     document.removeEventListener("contextmenu", (e) => e.preventDefault());
    //   };
    // }, []);
  return (
    <>
    </>
  )
}

export default RemoveContexetMenu
